layer "bgLayer"
layer "mainLayer"

Font font (name = "Default font")
{
	font32 Antialiased 32
	font18 Antialiased 18
	font14 Antialiased 14
}

Texture2D "defaultImage" (path = "assets/default/default.png")
Texture2D "logo" (path = "assets/logo.png")
Texture2D "cursor" (path = "assets/cursor.png")

element TextButton "createElementButton" ("mainLayer")
{
	anchorAlignment	= TopLeft
	localAlignment	= TopLeft
	size			= (130, 30)
	pos				= (5, -5)
	
	borderColor		= 0x444444ff
	borderWidth		= 0
	cornerRadius	= 3

	text			= "New Element"
	font			= "font"
	fontSize		= 18

	pressed			= "createElementPressed"
}

element Panel "elementsPanel" ("bgLayer") 
{
	anchorAlignment	= TopLeft
	localAlignment	= TopLeft
	size			= (200, 500)
	pos				= (5, -40)

	fillColor		= 0x333333ee	
	borderWidth		= 0
	cornerRadius	= 3
}

element List "elementsList" ("mainLayer")
{
	anchor			= "elementsPanel"
	anchorAlignment	= TopLeft
	localAlignment	= TopLeft
	pos				= (10, -10)
}